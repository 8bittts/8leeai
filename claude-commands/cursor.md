<!-- Backup: claude-commands/ in yenchat, deathnote, 8leeai. Sync all 3 on any change. -->
Delegate a task to Cursor Agent CLI in headless mode.

Run the following command and capture the output:

```
agent -p --yolo --trust --approve-mcps --model composer-1.5 --output-format stream-json --stream-partial-output "$ARGUMENTS"
```

After it completes, summarize what Cursor Agent did and any results it produced.
