<!-- Backup: claude-commands/ in yenchat, deathnote, 8leeai. Sync all 3 on any change. -->
Delegate a task to OpenAI Codex CLI in headless mode.

Run the following command and capture the output:

```
codex exec --model gpt-5.3-codex --yolo "$ARGUMENTS"
```

After it completes, summarize what Codex did and any results it produced.
